{
  entries(
    first: 10
    orderBy: [
      { field: "price", fieldType: DECIMAL, direction: DESC_NULLS_LAST }
    ]
  ) {
    count
    edges {
      node {
        slug
        title
        decimalValues {
          fieldId
          value
        }
      }
    }
  }
}
